---
title: Closures
description: Hiểu về closures và lexical scope trong JavaScript
---

# Closures

Closure là một trong những concept quan trọng nhất trong JavaScript. Nó cho phép function truy cập variables từ outer scope ngay cả sau khi outer function đã return.

## Closure là gì?

```javascript
function outer() {
	const message = "Hello";

	function inner() {
		console.log(message); // Truy cập được biến từ outer function
	}

	return inner;
}

const myFunction = outer();
myFunction(); // "Hello"
```

**Closure = Function + Lexical Environment**

## Lexical Scope

Scope được xác định tại thời điểm code được viết:

```javascript
const global = "Global";

function outer() {
	const outerVar = "Outer";

	function middle() {
		const middleVar = "Middle";

		function inner() {
			const innerVar = "Inner";

			// Inner có thể truy cập tất cả:
			console.log(innerVar); // "Inner"
			console.log(middleVar); // "Middle"
			console.log(outerVar); // "Outer"
			console.log(global); // "Global"
		}

		return inner;
	}

	return middle;
}
```

## Tại sao Closures hữu ích?

### 1. Private Variables

```javascript
function createCounter() {
	let count = 0; // Private variable

	return {
		increment() {
			count++;
			return count;
		},
		decrement() {
			count--;
			return count;
		},
		getCount() {
			return count;
		},
	};
}

const counter = createCounter();
console.log(counter.increment()); // 1
console.log(counter.increment()); // 2
console.log(counter.getCount()); // 2

console.log(counter.count); // undefined - không truy cập trực tiếp được!
```

### 2. Function Factories

```javascript
function createGreeting(greeting) {
	return function (name) {
		return `${greeting}, ${name}!`;
	};
}

const sayHello = createGreeting("Hello");
const sayHi = createGreeting("Hi");

console.log(sayHello("Phi")); // "Hello, Phi!"
console.log(sayHi("Phi")); // "Hi, Phi!"
```

### 3. Event Handlers

```javascript
function setupButtons() {
	const buttons = document.querySelectorAll("button");

	for (let i = 0; i < buttons.length; i++) {
		const button = buttons[i];

		button.addEventListener("click", function () {
			console.log(`Button ${i} clicked`); // Closure giữ giá trị i
		});
	}
}
```

## Ví dụ thực tế

### Module Pattern

```javascript
const Calculator = (function () {
	// Private
	let result = 0;

	function log(operation, value) {
		console.log(`${operation} ${value} = ${result}`);
	}

	// Public API
	return {
		add(num) {
			result += num;
			log("Added", num);
			return this;
		},
		subtract(num) {
			result -= num;
			log("Subtracted", num);
			return this;
		},
		getResult() {
			return result;
		},
		reset() {
			result = 0;
			return this;
		},
	};
})();

Calculator.add(10).subtract(3);
console.log(Calculator.getResult()); // 7
```

### Debounce Function

```javascript
function debounce(func, delay) {
	let timeoutId;

	return function (...args) {
		clearTimeout(timeoutId);

		timeoutId = setTimeout(() => {
			func.apply(this, args);
		}, delay);
	};
}

// Sử dụng
const handleSearch = debounce((query) => {
	console.log("Searching for:", query);
}, 500);

// Chỉ chạy sau 500ms không có input mới
handleSearch("javascript");
handleSearch("javascript closures");
```

### Memoization

```javascript
function memoize(fn) {
	const cache = {};

	return function (...args) {
		const key = JSON.stringify(args);

		if (key in cache) {
			console.log("From cache");
			return cache[key];
		}

		const result = fn.apply(this, args);
		cache[key] = result;
		return result;
	};
}

const fibonacci = memoize(function (n) {
	if (n <= 1) return n;
	return fibonacci(n - 1) + fibonacci(n - 2);
});

console.log(fibonacci(10)); // Tính toán
console.log(fibonacci(10)); // From cache
```

## Common Pitfalls

### Loop + Closure với var

```javascript
// ❌ Problem
for (var i = 0; i < 3; i++) {
	setTimeout(function () {
		console.log(i); // 3, 3, 3 (không phải 0, 1, 2)
	}, 1000);
}

// ✅ Solution 1: Dùng let
for (let i = 0; i < 3; i++) {
	setTimeout(function () {
		console.log(i); // 0, 1, 2
	}, 1000);
}

// ✅ Solution 2: IIFE
for (var i = 0; i < 3; i++) {
	(function (j) {
		setTimeout(function () {
			console.log(j); // 0, 1, 2
		}, 1000);
	})(i);
}
```

### Memory Leaks

```javascript
// ❌ Closure giữ reference → memory leak
function setupHandler() {
	const bigData = new Array(1000000).fill("data");

	document.getElementById("button").addEventListener("click", function () {
		console.log("Clicked");
		// bigData vẫn tồn tại trong memory!
	});
}

// ✅ Chỉ giữ những gì cần
function setupHandler() {
	const bigData = new Array(1000000).fill("data");
	const summary = bigData.length; // Chỉ giữ length

	document.getElementById("button").addEventListener("click", function () {
		console.log(`Data length: ${summary}`);
		// bigData được garbage collected
	});
}
```

## Bài tập

```javascript
// 1. Tạo counter với reset
function createCounter(initialValue = 0) {
	let count = initialValue;

	return {
		increment: () => ++count,
		decrement: () => --count,
		reset: () => (count = initialValue),
		value: () => count,
	};
}

// 2. Once function - chỉ chạy 1 lần
function once(fn) {
	let called = false;
	let result;

	return function (...args) {
		if (!called) {
			result = fn.apply(this, args);
			called = true;
		}
		return result;
	};
}

const initialize = once(() => {
	console.log("Initialized!");
	return { status: "ready" };
});

initialize(); // "Initialized!"
initialize(); // Không log gì

// 3. Private properties
function createPerson(name, age) {
	return {
		getName: () => name,
		getAge: () => age,
		haveBirthday() {
			age++;
			return age;
		},
	};
}
```

## Tóm tắt

-   **Closure**: Function + lexical environment
-   **Tạo private data**: Variables không thể truy cập từ bên ngoài
-   **Function factories**: Tạo functions với pre-configured values
-   **Cẩn thận**: Memory leaks, loop với var

## Bước tiếp theo

-   👉 [Async/Await](/languages/javascript/advanced/async-await/)
-   📚 [Event Loop](/languages/javascript/advanced/event-loop/)
