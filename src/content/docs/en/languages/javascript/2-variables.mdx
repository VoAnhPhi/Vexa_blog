---
title: Variables in JavaScript
description: Variable declarations, hoisting, naming rules, and scopes
---

import { Card, CardGrid, Aside, Tabs, TabItem, Steps } from '@astrojs/starlight/components';

## Variable Declaration Keywords

JavaScript provides three keywords for declaring variables: `var`, `let`, and `const`. Each has different characteristics and use cases.

---

<Tabs>
  <TabItem label="var">
    The `var` statement declares a variable with **function scope** or **global scope**. Variables can be initialized with a value or left undefined.

    <Aside type="caution" title="Legacy Keyword">
    `var` is considered outdated. Modern JavaScript prefers `let` and `const` for better scoping and fewer bugs.
    </Aside>

    **1. Hoisting Behavior** — Variables declared with `var` are hoisted to the top of their scope, but their value is not assigned until execution reaches the assignment.

    ```js
    console.log(a); // undefined (no error!)
    var a = 10;
    console.log(a); // 10
    ```

    **2. Function Scope (Not Block Scope)** — `var` is only limited to function scope, **not** block scope.

    ```js
    if (true) {
        var x = 5;
    }
    console.log(x); // 5 (accessible outside the block!)
    ```

    **3. Can Be Re-declared**

    ```js
    var name = "Alice";
    var name = "Bob"; // No error
    console.log(name); // "Bob"
    ```
  </TabItem>
  <TabItem label="let">
    The `let` statement declares a variable with **block scope**. Variables can be initialized or left undefined.

    <Aside type="tip" title="Modern Choice">
    Use `let` when you need a variable whose value will change. It's safer than `var` because it respects block scope.
    </Aside>

    **1. Block Scope** — `let` variables only exist within the block `{}` where they're declared.

    ```js
    if (true) {
        let y = 10;
        console.log(y); // 10
    }
    console.log(y); // ReferenceError: y is not defined
    ```

    **2. Temporal Dead Zone (TDZ)** — While `let` variables are hoisted, they cannot be accessed before declaration.

    ```js
    console.log(z); // ReferenceError
    let z = 20;
    ```

    **3. Cannot Be Re-declared**

    ```js
    let name = "Alice";
    let name = "Bob"; // SyntaxError: Identifier 'name' has already been declared
    ```
  </TabItem>
  <TabItem label="const">
    The `const` statement declares a variable with **block scope** that **cannot be reassigned** after initialization.

    <Aside type="caution" title="Must Be Initialized">
    `const` variables must be assigned a value when declared. You cannot declare a `const` without an initial value.
    </Aside>

    **1. Must Be Initialized**

    ```js
    const pi; // SyntaxError: Missing initializer in const declaration
    const pi = 3.14; // Correct
    ```

    **2. Cannot Be Reassigned**

    ```js
    const pi = 3.14;
    pi = 3.14159; // TypeError: Assignment to constant variable
    ```

    **3. Objects and Arrays Are Mutable** — you cannot reassign the variable itself, but you **can modify the contents** of objects and arrays.

    ```js
    const arr = [1, 2, 3];
    arr.push(4);      // OK - modifying contents
    arr[0] = 10;      // OK - modifying contents
    arr = [10, 20];   // TypeError

    const person = { name: "Alice" };
    person.name = "Bob"; // OK - modifying property
    person = {};         // TypeError
    ```
  </TabItem>
</Tabs>

---

### Comparison Table

| Keyword   | Scope           | Hoisting        | Re-declaration | Reassignment   | Notes                                 |
| --------- | --------------- | --------------- | -------------- | -------------- | ------------------------------------- |
| **var**   | Function/Global | Yes (undefined) | Allowed        | Allowed        | Not limited to block `{}`             |
| **let**   | Block `{}`      | Yes (TDZ)       | Not allowed    | Allowed        | Safer than `var`                      |
| **const** | Block `{}`      | Yes (TDZ)       | Not allowed    | Not allowed    | Use for constants or fixed references |

<Aside type="tip" title="Best Practice">
- Use `const` by default
- Use `let` if you need to reassign the variable
- Avoid `var` in modern JavaScript
</Aside>

---

## Hoisting

### What is Hoisting?

**Hoisting** is a JavaScript mechanism where variable and function **declarations** are moved to the top of their scope during the compilation phase, before code execution.

<Aside type="note" title="Only Declarations Are Hoisted">
Only the declaration is hoisted, **not the initialization**. The value assignment stays in place.
</Aside>

### Hoisting with `var`

```js
console.log(myVar); // undefined
var myVar = 5;
console.log(myVar); // 5

// Interpreted as:
// var myVar;           // Declaration hoisted
// console.log(myVar);  // undefined
// myVar = 5;           // Assignment stays here
// console.log(myVar);  // 5
```

### Hoisting with `let` and `const` (TDZ)

```js
console.log(myLet);   // ReferenceError
let myLet = 10;

console.log(myConst); // ReferenceError
const myConst = 20;
```

### Hoisting with Functions

**Function declarations** are fully hoisted (including the function body).

```js
sayHello(); // "Hello!" - works!

function sayHello() {
    console.log("Hello!");
}
```

<Aside type="tip" title="Function Expressions Are Not Fully Hoisted">
If you assign a function to a variable, only the variable declaration is hoisted, not the function itself.
</Aside>

```js
greet(); // TypeError: greet is not a function

var greet = function() {
    console.log("Hi!");
};
```

### Summary

<CardGrid>
  <Card title="var" icon="warning">
    Declaration hoisted, value is `undefined`
  </Card>
  <Card title="let, const" icon="error">
    Declaration hoisted, but in TDZ (cannot access before declaration)
  </Card>
  <Card title="function declaration" icon="approve-check">
    Fully hoisted (including body)
  </Card>
  <Card title="function expression" icon="warning">
    Only variable declaration hoisted
  </Card>
</CardGrid>

---

## Variable Naming Rules

Choosing good variable names makes your code more readable and maintainable.

### JavaScript Naming Conventions

| Convention | Usage | Example |
| ---------- | ----- | ------- |
| **camelCase** | Standard for JS variables/functions | `firstName`, `userAge`, `isLoggedIn` |
| **PascalCase** | Classes/Constructors | `UserProfile`, `ShoppingCart` |
| **snake_case** | Not common in JS | `first_name`, `user_age` |
| **SCREAMING_SNAKE_CASE** | True constants | `MAX_VALUE`, `API_KEY`, `DEFAULT_TIMEOUT` |

---

## Scopes (Variable Visibility)

**Scope** determines where variables are accessible in your code.

### Three Main Scopes

<CardGrid stagger>
  <Card title="Global Scope" icon="globe">
    Accessible everywhere in the file/program — `var`, `let`, `const`
  </Card>
  <Card title="Function Scope" icon="seti:javascript">
    Accessible only inside the function — `var`, `let`, `const`
  </Card>
  <Card title="Block Scope" icon="seti:folder">
    Accessible only inside `{}` — `let`, `const` only
  </Card>
</CardGrid>

### Examples

<Tabs>
  <TabItem label="Global Scope">
    ```js
    let globalVar = "I'm global";

    function test() {
        console.log(globalVar); // Accessible
    }

    test();                    // "I'm global"
    console.log(globalVar);    // "I'm global"
    ```
  </TabItem>
  <TabItem label="Function Scope">
    ```js
    function myFunction() {
        var functionVar = "I'm in a function";
        console.log(functionVar); // Accessible
    }

    myFunction();
    console.log(functionVar); // ReferenceError
    ```
  </TabItem>
  <TabItem label="Block Scope">
    ```js
    if (true) {
        let blockVar = "I'm in a block";
        const blockConst = "I'm also in a block";
        console.log(blockVar); // Accessible
    }

    console.log(blockVar);   // ReferenceError
    console.log(blockConst); // ReferenceError
    ```
  </TabItem>
</Tabs>

<Aside type="caution" title="var Does Not Respect Block Scope">
This is one of the main reasons to avoid `var` in modern JavaScript.

```js
if (true) {
    var leakyVar = "I leak out!";
}
console.log(leakyVar); // "I leak out!" (not recommended!)
```
</Aside>

---

## Next Steps

<Aside type="tip" title="Continue Learning">
Now that you understand variables, continue to:

👉 [Data Types](/en/languages/javascript/3-data-types/) — Learn about primitive and non-primitive data types in JavaScript.
</Aside>
