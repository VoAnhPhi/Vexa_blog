---
title: Data Types in JavaScript
description: Primitive types, Objects, Prototypes, and Built-in Objects
---

# Data Types in JavaScript

JavaScript has two main categories of data types: **Primitive types** and **Non-primitive types** (Objects).

---

## 🔹 Primitive Types (7 Types)

Primitive types are the most basic data types in JavaScript. They are **immutable** (cannot be changed) and are compared by value.

| Type          | Icon | Description                     |
| ------------- | ---- | ------------------------------- |
| **number**    | 🔢   | Numbers (integers and decimals) |
| **bigint**    | 🔢➕ | Very large integers             |
| **string**    | 📝   | Text/characters                 |
| **boolean**   | ✅❌ | True or false values            |
| **null**      | ⭕   | Intentional absence of value    |
| **undefined** | ❓   | Unassigned value                |
| **symbol**    | 🔣   | Unique identifiers              |

### Characteristics of Primitive Types

> [!NOTE] > **Key Properties**
>
> -   **Immutable** - Values cannot be changed
> -   **Compared by value** - Two variables with the same value are equal
> -   **Copied by value** - Assignments copy the value, not a reference

---

## 1️⃣ Number

Includes integers, decimals, `NaN`, and `Infinity`. Safe integer range: ±(2⁵³-1).

```js
let age = 25; // Integer
let pi = 3.14; // Decimal
let notANumber = NaN; // Not a Number
let infinity = Infinity; // Infinity
let negInfinity = -Infinity;

console.log(typeof age); // "number"
console.log(typeof NaN); // "number" (quirk!)
```

> [!TIP] > **Checking for NaN**
> Use `isNaN()` or `Number.isNaN()` to check if a value is NaN. Don't use `=== NaN` because `NaN !== NaN` in JavaScript!

```js
console.log(NaN === NaN); // false
console.log(isNaN(NaN)); // true
console.log(Number.isNaN(NaN)); // true (recommended)
```

---

## 2️⃣ BigInt

For integers larger than the safe integer limit. Denoted with `n` suffix.

```js
let bigNumber = 123n;
let veryBig = 9007199254740991n;

console.log(typeof bigNumber); // "bigint"
```

> [!WARNING] > **Cannot Mix BigInt and Number**
> You cannot perform arithmetic operations between BigInt and Number directly.

```js
1n + 1; // ❌ TypeError
1n + BigInt(1); // ✅ 2n
```

---

## 3️⃣ String

Text data encoded in UTF-16. Strings are **immutable**.

```js
let name = "JavaScript";
let greeting = "Hello World";
let template = `Hello ${name}`; // Template literal

console.log(typeof name); // "string"
```

### String Methods

```js
let str = "  Hello World  ";

str.length; // 15
str.toUpperCase(); // "  HELLO WORLD  "
str.toLowerCase(); // "  hello world  "
str.trim(); // "Hello World"
str.includes("World"); // true
str.split(" "); // ["", "", "Hello", "World", "", ""]
```

---

## 4️⃣ Boolean

Only two values: `true` or `false`.

```js
let isActive = true;
let isComplete = false;

console.log(typeof isActive); // "boolean"
```

### Truthy and Falsy Values

> [!IMPORTANT] > **Falsy Values**
> Only these 6 values are falsy in JavaScript. Everything else is truthy!

```js
// Falsy values:
Boolean(false); // false
Boolean(0); // false
Boolean(-0); // false
Boolean(""); // false
Boolean(null); // false
Boolean(undefined); // false
Boolean(NaN); // false

// Truthy examples:
Boolean("hello"); // true
Boolean(42); // true
Boolean([]); // true (empty array is truthy!)
Boolean({}); // true (empty object is truthy!)
```

---

## 5️⃣ Null

Represents the **intentional absence** of a value.

```js
let emptyValue = null;
console.log(typeof null); // "object" (historical bug!)
```

> [!WARNING] > **typeof null Bug** > `typeof null` returns `"object"` due to a longstanding bug in JavaScript. `null` is actually a primitive type, not an object!

---

## 6️⃣ Undefined

Represents a variable that has been declared but **not assigned** a value.

```js
let notAssigned;
console.log(notAssigned); // undefined
console.log(typeof notAssigned); // "undefined"

function test() {}
console.log(test()); // undefined (function returns nothing)
```

### null vs undefined

| Aspect         | null                   | undefined                           |
| -------------- | ---------------------- | ----------------------------------- |
| **Meaning**    | Intentional absence    | Not yet assigned                    |
| **typeof**     | "object" (bug)         | "undefined"                         |
| **Assignment** | Must be explicitly set | Default for uninitialized variables |
| **Equality**   | `null == undefined` ✅ | `null === undefined` ❌             |

---

## 7️⃣ Symbol

Creates **unique** identifiers. Often used for object property keys.

```js
let id = Symbol("id");
let anotherId = Symbol("id");

console.log(id === anotherId); // false (each symbol is unique!)
console.log(typeof id); // "symbol"
```

---

## 🔍 The typeof Operator

Check the type of a value using `typeof`:

```js
typeof 42; // "number"
typeof 123n; // "bigint"
typeof "hello"; // "string"
typeof true; // "boolean"
typeof undefined; // "undefined"
typeof Symbol("id"); // "symbol"
typeof null; // "object" ⚠️ (bug!)
typeof {}; // "object"
typeof []; // "object"
typeof function () {}; // "function"
```

> [!TIP] > **Checking for Arrays**
> Use `Array.isArray()` to check if something is an array, since `typeof` returns `"object"` for arrays.

```js
typeof []; // "object"
Array.isArray([]); // true
```

---

## 🔸 Non-Primitive Types (Objects)

Non-primitive types (also called **reference types**) include Objects, Arrays, Functions, and more. They are stored as **references**, not values.

### What Are Objects?

Objects store collections of data as **key-value pairs**.

```js
// Object literal (recommended)
let person = {
	name: "Alice",
	age: 25,
	greet: function () {
		console.log(`Hello, I'm ${this.name}`);
	},
};

// Accessing properties
console.log(person.name); // "Alice" (dot notation)
console.log(person["age"]); // 25 (bracket notation)

person.greet(); // "Hello, I'm Alice"
```

### Common Object Operations

```js
// Create
let obj = { a: 1, b: 2 };

// Clone (shallow copy)
let clone1 = Object.assign({}, obj);
let clone2 = { ...obj }; // Spread operator

// Iterate
Object.keys(obj); // ["a", "b"]
Object.values(obj); // [1, 2]
Object.entries(obj); // [["a", 1], ["b", 2]]

// Check property
"a" in obj; // true
obj.hasOwnProperty("a"); // true

// Delete property
delete obj.a;

// Freeze (make immutable)
Object.freeze(obj);
```

---

## 🧬 Prototype

Every object in JavaScript has a hidden **prototype** object that it inherits from. When you access a property, JavaScript first looks in the object itself, then in its prototype chain.

### Example

```js
function Person(name) {
	this.name = name;
}

// Add method to prototype
Person.prototype.sayHello = function () {
	console.log(`Hello, I'm ${this.name}`);
};

let john = new Person("John");
john.sayHello(); // "Hello, I'm John"

// john doesn't have sayHello directly
console.log(john.hasOwnProperty("sayHello")); // false

// But it's in the prototype
console.log(john.__proto__ === Person.prototype); // true
```

> [!NOTE] > **Prototype Chain**
> When you access `john.sayHello()`, JavaScript:
>
> 1. Looks for `sayHello` in `john` → not found
> 2. Looks in `john.__proto__` (which is `Person.prototype`) → found!

---

## 🔗 Prototype Chain (Prototypal Inheritance)

**Prototypal inheritance** allows objects to inherit properties and methods from other objects through the prototype chain.

### Example: Animal → Dog

```js
// Parent constructor
function Animal(name) {
	this.name = name;
}

Animal.prototype.speak = function () {
	console.log(`${this.name} makes a sound`);
};

// Child constructor
function Dog(name, breed) {
	Animal.call(this, name); // Call parent constructor
	this.breed = breed;
}

// Set up inheritance
Dog.prototype = Object.create(Animal.prototype);
Dog.prototype.constructor = Dog;

// Add Dog-specific method
Dog.prototype.bark = function () {
	console.log(`${this.name} barks!`);
};

// Create instance
let dog = new Dog("Buddy", "Golden Retriever");
dog.speak(); // "Buddy makes a sound" (inherited from Animal)
dog.bark(); // "Buddy barks!" (Dog's own method)
```

> [!TIP] > **Modern Alternative: Classes**
> ES6 introduced the `class` syntax, which is syntactic sugar over prototypes but easier to read.

```js
class Animal {
	constructor(name) {
		this.name = name;
	}
	speak() {
		console.log(`${this.name} makes a sound`);
	}
}

class Dog extends Animal {
	constructor(name, breed) {
		super(name);
		this.breed = breed;
	}
	bark() {
		console.log(`${this.name} barks!`);
	}
}

let dog = new Dog("Buddy", "Golden Retriever");
dog.speak(); // "Buddy makes a sound"
dog.bark(); // "Buddy barks!"
```

---

## 📦 Built-in Objects

JavaScript provides many built-in objects that offer useful methods and properties.

| Object      | Purpose                  | Common Methods/Properties                                                                              |
| ----------- | ------------------------ | ------------------------------------------------------------------------------------------------------ |
| **Number**  | Work with numbers        | `toFixed()`, `toPrecision()`, `isNaN()`, `isFinite()`, `parseInt()`, `parseFloat()`                    |
| **Math**    | Mathematical operations  | `Math.PI`, `Math.sqrt()`, `Math.pow()`, `Math.floor()`, `Math.ceil()`, `Math.round()`, `Math.random()` |
| **Date**    | Work with dates/times    | `getFullYear()`, `getMonth()`, `getDate()`, `getHours()`, `toDateString()`, `toISOString()`            |
| **String**  | String manipulation      | `length`, `toUpperCase()`, `toLowerCase()`, `substring()`, `split()`, `trim()`, `includes()`           |
| **Array**   | Array operations         | `push()`, `pop()`, `map()`, `filter()`, `reduce()`, `forEach()`, `find()`, `includes()`, `sort()`      |
| **Object**  | Object utilities         | `keys()`, `values()`, `entries()`, `assign()`, `freeze()`, `seal()`                                    |
| **Map**     | Key-value collections    | `set()`, `get()`, `has()`, `delete()`, `clear()`, `size`                                               |
| **Set**     | Unique value collections | `add()`, `delete()`, `has()`, `clear()`, `size`                                                        |
| **RegExp**  | Regular expressions      | `test()`, `exec()`, `match()`, `replace()`                                                             |
| **Promise** | Async operations         | `then()`, `catch()`, `finally()`, `Promise.all()`, `Promise.race()`                                    |

### Examples

```js
// Math
Math.PI; // 3.141592653589793
Math.sqrt(16); // 4
Math.random(); // Random number between 0 and 1
Math.floor(4.7); // 4
Math.ceil(4.3); // 5

// Array
let arr = [1, 2, 3, 4, 5];
arr.map((x) => x * 2); // [2, 4, 6, 8, 10]
arr.filter((x) => x > 2); // [3, 4, 5]
arr.reduce((a, b) => a + b); // 15

// Date
let now = new Date();
now.getFullYear(); // 2024
now.getMonth(); // 0-11 (0 = January)
now.toISOString(); // "2024-01-01T00:00:00.000Z"

// Set (unique values)
let set = new Set([1, 2, 2, 3, 3, 3]);
console.log(set); // Set { 1, 2, 3 }
set.has(2); // true
set.size; // 3
```

> [!NOTE] > **Always Available**
> Built-in objects are part of the global scope and can be used anywhere in your code without importing or requiring them.

---

## 🚀 Next Steps

Continue your learning journey:

👉 **Next:** [Type Casting](/en/languages/javascript/4-type-casting/)

Learn about implicit and explicit type conversions in JavaScript.
